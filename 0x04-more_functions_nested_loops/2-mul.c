#include "main.h"

/**
 * mul - Multiples two integers.
 * @k: The first integer to be multiplied.
 * @h: The second integer to be multiplied
 * Return: The result of the multiplication.
 */
int mul(int k, int h)
{
	return (k * h);
}
